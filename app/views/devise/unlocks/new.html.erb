<h2>Reenviar e-mail de desbloqueio.</h2>

<%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <%= f.email_field :email %>

  <div><%= f.submit "Reenviar" %></div>
<% end %>

<%= render "devise/shared/links" %>
